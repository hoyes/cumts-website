* {
    border:none;
    margin:0;
    padding:0;
}

html {
    height:100%;
}

body {
    background: #000;
    font-family:"lucida grande",tahoma,verdana,arial,sans-serif;
    font-size:13px;
    height:100%;
    
}

#wrapper {
    min-height:100%;
    background:#fdd;
    margin:auto;
    width:960px;
    position:relative;
}

header {
    width:940px;
    margin: 0 auto;
    padding:5px 10px;
    background:#a01;
}

header h1 {
    
}

nav {
    margin:auto;
    width:940px;
    padding:0 10px;
    background: #800;
    border-top:2px solid #faa;
    border-bottom:2px solid #a00;
    font-size:16px;
}

nav ul {
    display:block;
}
nav ul li {
    display:block;
    list-style:none;
    float: left;
}
nav ul li a {
    display:block;
    padding:0.3em 0.8em;
    padding-left:25px;
    text-decoration: none;
    color: #fdd;
    background:url("/bundles/cumtsmain/images/star.png") no-repeat center left;
}
nav ul li a:hover {
    color: #fff;
    background-image:url("/bundles/cumtsmain/images/star_over.png")
}

section {
    width:940px;
    margin: 0 auto;
    background:#fdd;
    clear:both;
    min-height:100px;
    font-size:13px;
    padding:10px;
    padding-bottom:40px;
}

section h1 {
    color:#a00;
    letter-spacing: 0.1em;
    font-size:1.6em;
    margin-bottom:0.3em;
}

section h2 {
    color:#a00;
    letter-spacing: 0.1em;
    font-size:1.1em;
    border-bottom:1px solid #f55;
}

section p {
    margin-bottom:0.8em;
}

section a:link, section a:visited {
    text-decoration:none;
    color:#f00;
}

section a:hover {
    color:#800;
}

footer {
    font-size:12px;
    text-align: center;
    margin: 0 auto;
    background: #a01;
    padding:0.5em 0;
    color:#fff;
    
    position:absolute;
    bottom:0;
    width:960px;
    height:18px;
}

.clear {
    clear:both;
}

form {
        width:800px;
}

form > div > div, form p {
    clear:both;
}

form label {
    float:left;
    display:block;
    width:250px;
    padding: 0.2em 0.5em;
    text-align:right;
    clear:left;
}

form input, form textarea, form > div > div > select, .form_static_text, form > div > div > div {
    margin-left:0.2em 0.5em;
    float:left;
    clear:none;
}

form ul {
        float:right;
        list-style:none;
        color:red;
        margin-left: 0.2em;
        margin-top:0.5em;
}
form ul li {
        margin-bottom:0.5em;
}

form select {
    margin:0.2em 0.5em;
}

form > div > div > div {
    margin-left:-0.2em;
}

form > div > div > div > div > select {
    margin: 0.2em;
}

form input, form textarea, form select {
    border:1px solid #000;
    padding:0.2em;
}

form input[type=text] {
    width:250px;
}

form input[type=number] {
    width:150px;
}

form textarea {
    width:400px;
    height:200px;
    font-family:Veranda, Arial, sans-serif;
}

form button {
    border:default;
    padding:0.3em 1em;
    margin-top:0.8em;
    margin-bottom:0.2em;
    background:#f55;
}

form p button {
    margin-left:270px;
}

form input:focus, form textarea:focus, form select:focus {
    border-color: #f00;
}
form:after {
    clear:both;
    content: ".";
    visibility:hidden;
    height:0;
    display:block;
}
